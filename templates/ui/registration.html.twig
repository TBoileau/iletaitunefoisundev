{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
        {{ form_errors(form) }}
        {{ form_rest(form) }}
        <button>S'inscrire</button>
    {{ form_end(form) }}
{% endblock %}
